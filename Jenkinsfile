pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                echo 'Checkout OK'
            }
        }

        stage('Build') {
            steps {
                echo 'Build OK'
            }
        }

        stage('SonarQube') {
            steps {
                echo 'Sonar OK'
            }
        }

        stage('Docker') {
            steps {
                echo 'Docker OK'
            }
        }
    }
}
